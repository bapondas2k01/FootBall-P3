name: football_controller
description: A simple Flutter app to control the local Phaser game over Wi-Fi.
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  web_socket_channel: ^2.2.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
